# eest-succ-allow.txt — intentionally empty.
#
# tests-zkevm@v0.6.1 fills statelessOutputBytes from run_stateless_guest
# (execution-specs commit 3f2859dc1), so the two former builder-request
# pins now record succ=0 natively.  The spec-vs-fixtures discrepancy tracked
# by issue #10182 is resolved.
#
# Format: one substring per line, matched against the case label
# (manifest column 1).  Lines starting with # are comments.
