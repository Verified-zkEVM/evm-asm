# Registry-coverage allowlist (GH #11637) — checked by scripts/check-registry-coverage.py.
#
# One entry per line: <symbol><TAB><reason>. An entry asserts that a LINKED,
# spec-bearing guest routine deliberately has NO row in either proof registry
# (Progress/Routines.lean or Progress/Correspondence.lean) — and says why.
#
# THE EXEMPTION EXPIRES. If the symbol gains a row, loses its routine-level spec,
# or leaves the guest image, the run fails with STALE and names the line to delete.
# That is the point: an exemption that outlives its reason is how a backlog goes
# silent again, which is the defect #11637 recorded (~103 linked, proven routines
# invisible to every census, so the proven-count understated reality).
#
# Seeded with the backlog as of 2026-08-06 at 103. Burn it down; do not grow it.
#
# ⭐ FIRST REAL BURN-DOWN, same day: #11575 registered `header_validate_extra_data_length`
# (a tier-A entry), and this gate failed with STALE naming the line to delete rather than
# letting the exemption outlive its reason. 103 -> 102. That is the ratchet working on a
# live merge, not a synthetic test.
#
# TIER A — a flat `cpsTripleWithin` at `GuestAddrs.<symbol>`. Registrable as
#   `.proven` in Routines.lean TODAY with no new proof work. These are the ones to
#   clear first; each deletion here is a row gained.
# TIER B — a structured SAsm `.Spec` only. `Fn.retSpecFlat` must derive the flat
#   whole-routine triple before a `.proven` row would be honest.
#   ⚠️ DO NOT grade these `.proven` to shorten this file. Registering a
#   structured-only spec as a flat triple is exactly the invisible overclaim this
#   gate exists to prevent — it would turn a visible backlog into a false census.
#
# ⚠️ THE TIER IN THIS COLUMN IS PROSE, AND IT HAS DRIFTED BEFORE. Verify against the
#   theorem STATEMENT before acting on it; a name is not a shape.
#   ⭐ Since #12226 you no longer have to do that by eye:
#       scripts/proof-frontier.py --shape
#   parses each theorem's CONCLUSION and buckets it whole-routine / structured-only /
#   fragment / model-only / needs-read.  Only `whole-routine` is tier A.
#
#   * false tier A, RETRACTED 2026-08-12 — five entries (`blsg_le_to_be`,
#     `secf_be_to_le`, `secf_is_zero32`, `secf_le_to_be`, `secf_zero32`) were moved to
#     tier B that same day on the ground that their statements conclude
#     `(…Fn …).Spec base` at a FREE base.  That read was WRONG and is now reverted:
#     each of the five ALSO has a `…Flat_spec` — a flat `cpsTripleWithin` at its own
#     `GuestAddrs.<sym>` — and the demotion only looked at the `Fn_spec`.
#     ⭐ WHY THE READ MISSED IT, and the lesson for the next one: the flat sibling is
#     NOT in the routine's own module.  It lives in the CALLER's stage file
#     (`secfZero32Flat_spec` is in Secp256k1PointDoubleSAsmStage.lean, not
#     Secp256k1FieldLeavesSAsm.lean), because that is where the stage `CodeReq` union
#     is assembled.  Grep the SYMBOL across EvmAsm/, never just its own module.
#     ⛔ PARTLY WRONG, corrected #12244 while rowing the last of the five.
#     `blsg_le_to_be`'s entry sent the reader to Bls12G2EncodeSAsm.lean (`encCr`) as
#     though the caller union were the only flat triple.  It was not: an OWN-CodeReq
#     triple (`blsgLeToBeCr = CodeReq.ofProg (GuestAddrs.blsg_le_to_be) blsgLeToBe_prog`)
#     had been sitting in the routine's own module all along, referenced by nothing —
#     so the symbol was rowable with ZERO proof work, not "after a lift".  The generalised
#     lesson is therefore the OPPOSITE of "look in the caller": look in BOTH, and grade by
#     the CodeReq, because a caller-union triple and an own-CodeReq triple can share a name
#     (three theorems here are called some form of `blsgLeToBeFlat_spec`).
#     ⚠️ And do not pipe the grep through `head` — truncation at 12 lines is exactly what
#     hid the own-module theorem on the first pass.
#     ⭐ ALL FIVE are now rowed (`secf_zero32`, `secf_is_zero32`, `secf_be_to_le`,
#     `secf_le_to_be` in #12244's earlier PRs; `blsg_le_to_be` here), so this block is
#     closed.
#   * ⭐ THE SAME DEFECT, one entry over — `blq_zero`, also rowed in #12244. Its line
#     named Bls12Fq12SetOneSAsm.lean, whose `blqCr` requires the CONCATENATION
#     `blqZero_prog ++ blqSetOne_prog` at this address: an adjacency claim about TWO
#     routines, stronger than the single-program image pairing.  The own-CodeReq triple
#     (`blqZeroCr`) was in Bls12Fq12ZeroSAsm.lean all along, and BOTH theorems are
#     called `blqZeroFlat_spec`.  So when an entry names a file, check the routine's OWN
#     module too, and grade by the CodeReq rather than the theorem name.
#     ⚠️ Remaining tier-A entries measured the same way on 2026-08-14:
#       - `bnf_be_to_le` / `bnf_le_to_be` — ⭐ CLOSED, both rowed in #12244. They were
#         union CodeReqs ONLY (two stage files each, `Bn254Field{Add,Mul}ModPSAsmStage`),
#         the same class the secp256k1 pair closed: name the own-CodeReq entry triple via
#         `Fn.retSpecFlat`, then hand each caller back its union version with
#         `liftCode … (by code_mem)`.  It cost NO new proof and DELETED 250 lines.
#         ⭐ THE CHEAP TEST, worth applying to any future `<sym>Flat_spec` that appears in
#         more than one caller: diff the copies with the CodeReq name normalised away.
#         Here they were BYTE-IDENTICAL (186 lines, `addCr` vs `mulCr`) — which is the
#         signature of the class, since the union is the only caller-specific part.  If
#         the copies agree, each already builds the own-CodeReq triple internally and the
#         symbol is rowable for free.  See `Bn254FieldConvFlatEntry.lean`.
#       - `bnq_zero` — ⭐ CLOSED, rowed in #12244, and the grading above was WRONG in a
#         way worth remembering. It said: adjacency CodeReq `CodeReq.ofProg
#         (GuestAddrs.bnq_zero) (bnqZero_prog ++ bnqSetOne_prog)` "WITHOUT an own-CodeReq
#         sibling — needs the sibling before a row is honest", i.e. real proof work.
#         Half right. There is no separately NAMED sibling, but the caller's
#         `bnqZeroFlat_spec` BUILDS the own-CodeReq triple internally via
#         `Fn.retSpecFlat` and widens it with `liftCode (cr' := bnqCr)` on the very next
#         line — exactly like the converter pairs.  So it was free: name the
#         intermediate step, in the ROUTINE'S OWN module, and hand the caller back its
#         adjacency version as a one-liner.
#         ⚠️ "No own-CodeReq sibling" is a claim about NAMES. Before concluding a lift
#         must be BUILT, read the caller's proof for the intermediate STEP — every
#         member of this class so far had it, unnamed, one line above a `liftCode`.
#         ⚠️ The lift here is PREFIX containment (`bnqZero_prog` is a prefix of the
#         concatenation), not union membership; and `code_mem` needs the named
#         `bnqZeroCr` unfolded (`simp only [bnqZeroCr]`) because it matches a LITERAL
#         `CodeReq.ofProg`.
#       - `eip8037_tx_state_gas`, `mset_memcpy` — NO `CodeReq.ofProg (GuestAddrs.<sym>)`
#         anywhere; their triples are anchored through some other base term. Each needs
#         its statement read before it can be graded.
#         ⛔ BOTH CLAIMS WERE FALSE, and both symbols are now rowed `.proven`.
#         `mset_memcpy`'s base was the file-local `msetMemcpyBase`, which UNFOLDS to
#         `GuestAddrs.mset_memcpy`; `eip8037_tx_state_gas`'s
#         `Eip8037TxStateGasSpec.P` is `BitVec.ofNat 64 GuestAddrs.eip8037_tx_state_gas`
#         written out in full. Neither was "some other base term" — both were the
#         anchor under a different spelling. Read what the abbrev unfolds to.
#     ⭐ CLOSED for FOUR of the five as of #12244: `secf_zero32`, `secf_is_zero32`,
#     `secf_be_to_le` and `secf_le_to_be` are rowed, each citing an own-`CodeReq`
#     `…FlatEntry_spec` sibling rather than the caller-anchored `…Flat_spec` these
#     entries pointed at.  `blsg_le_to_be` (below) is the LAST member and needs no new
#     insight: the recipe is `Fn.retSpecFlat` at the routine's OWN program, then
#     `liftCode … (by code_mem)` to hand each caller back its union version as a
#     one-liner.  See Secp256k1FieldConvFlatEntry.lean for the worked pair — doing it
#     that way DELETED ~360 lines of duplicated proof instead of adding a copy.
#   * false tier B — `u256_lt_be` was listed tier B, but `u256LtBe_spec` is a flat
#     `cpsTripleWithin` at `CodeReq.ofProg (GuestAddrs.u256_lt_be) u256LtBe_prog`; the
#     module was flattened after the line was written. Rowed 2026-08-12.
#     ⚠️ NOT a one-off: `--shape` flagged 19 further tier-B lines whose statements parse
#     as whole-routine. SEVEN have since been read individually and ROWED `.proven`
#     (bloom_eq, blq_eq, bnq_eq, bnp_fp2_eq, blsg2_eq_n, frame_base, u256_min); their
#     lines are gone from this file, which is the ratchet working.
#     ⛔ "TWELVE remain" IS STALE AND WAS AN UNDERCOUNT. Re-measured 2026-08-17 with
#     `proof-frontier.py --shape`: **28** present-but-unrowed symbols parse as
#     whole-routine, not 12 -- flat triples were added for many of these after the note
#     was written, and nothing updates the note. The list that used to be here
#     (blsg2_encode, blsk_g2_wire, blsk_lt_be, bn254_call_allotment, bnf_add_mod_p,
#     dispatcher_capture_exec_state_gas, frame_depth_{pop,push},
#     frame_{load,save}_regs, mpt_resolve_cache_reset, p256_lt_be) was a SUBSET.
#     ⭐ So run `--shape` for the queue; do not read a count out of this file.
#     Of the 28, all 28 have a `guestImageEntries` pairing and a UNIQUE theorem name,
#     and 25 resolve to a single OWN-program CodeReq -- checked by resolving each named
#     `...Cr` abbrev to its components and testing every component against the image,
#     the code-identity check `--shape` does NOT do (see the `eip8037_tx_state_gas`
#     note under the TIER A header below; its entry was deleted when #12244 rowed it).
#     The 3 exceptions are the composite CALLERS blsg2_encode, blsk_g2_wire, bnf_add_mod_p.
#     ⚠️ A DISTINCTION THAT MATTERS FOR THOSE THREE: a union CodeReq is NOT
#     automatically unrowable. For a LEAF the union is an artifact of where the proof
#     lived (the #12389/#12516 class). For a routine whose body actually `jal`s to
#     callees the union is SEMANTICALLY REQUIRED -- it cannot execute without them --
#     and the image discharges it, just via several pairings instead of one. So the
#     test is not "is it a bare ofProg" but "is EVERY component a real image pairing
#     at the address the union names".
#     ⭐ FOUR CLOSED here (#12244): the frame-port family frame_depth_push,
#     frame_depth_pop, frame_save_regs, frame_load_regs. Their tier-B reason ("needs
#     Fn.retSpecFlat before a .proven row is honest") was FALSE -- there is no `Fn` in
#     those modules at all; the triples are hand-built straight-line compositions with
#     `<sym>_byte_tie ... := by rfl`. Read the module, not the tier.
#     ⭐ FOUR MORE CLOSED (#12244): the P-256 family p256_be_to_le, p256_le_to_be,
#     p256_copy_n, p256_lt_be -- and their reasons were stale in a DIFFERENT way, worth
#     distinguishing because the fix differs:
#       * frame family  -- "needs Fn.retSpecFlat" with NO `Fn` in the module at all.
#       * p256 be/le, le/be, copy_n -- there IS an `Fn`, and `Fn.retSpecFlat` had
#         ALREADY been applied; the `...Flat_spec` sits in the SAME FILE as the
#         `...Fn_spec` the entry cited. The entry named the weaker sibling.
#       * p256_lt_be -- called "structured SAsm spec" when `p256LtBe_spec` is a flat
#         triple with an INLINE `CodeReq.ofProg ltPBase p256LtBe_prog` (no named `...Cr`
#         abbrev, which is likely why a name-based scan missed it).
#     ⭐ So the rule is not "distrust the tier" but "cite the theorem you actually read,
#     and read the WHOLE file" -- the flat sibling is often a few lines below.
#     ⭐ EIGHT MORE CLOSED (#12244): the BLS12 leaf family blq_copy, blq_pt_copy,
#     blsg_copy96, blsf_copy_quads, blsg_zero96, blsg2_zero192, blq_is_zero,
#     blsg_is_zero_n -- all the p256-mover flavour (Fn.retSpecFlat ALREADY applied, the
#     ...Flat_spec sitting below the ...Fn_spec the entry cited).
#     ⚠️ Grading note for the copiers: their domain hypothesis is named `frameOk96` /
#     `frameOk576` / `frameOk1728` / `frameOkN`, which UNFOLDS to both-bases-
#     non-overflowing AND window-disjointness. So a named predicate can hide exactly the
#     `hdisj` restriction that makes a triple non-total -- unfold it before calling a row
#     total. The two zeroers have one live window and genuinely are total.
#     ⭐ THE LAST NINE CLOSED (#12244): bgv_u64le, blk2_ld_le64, blk2_st_le64,
#     bloom_or_into, blsk_lt_be, bn254_call_allotment,
#     dispatcher_capture_exec_state_gas, hp_encode_nibbles, mpt_resolve_cache_reset.
#     That is all 25 of the 28 --shape whole-routine symbols that resolved to a single
#     own-program CodeReq; only the 3 composite CALLERS remain (blsg2_encode,
#     blsk_g2_wire, bnf_add_mod_p), which need the union-vs-image test described above.
#     ⛔ NAMING TRAP FOUND WHILE GRADING THEM, worth more than the rows: `ltPBase` is
#     defined FOUR times in the tree -- Bls12KzgLtBeSAsm (=blsk_lt_be), Bls12G1LtPSAsm
#     (=blsg_lt_p), P256LtBeSAsm (=p256_lt_be), Bn254FieldLtPSAsm (=bnf_lt_p) -- each a
#     DIFFERENT guest address. So `CodeReq.ofProg ltPBase ...` is meaningless until the
#     namespace is known, and any tool that grades a CodeReq by its surface text must
#     resolve base abbrevs per-module. `leU64` likewise exists three times (one with a
#     different arity). Cite namespaces, not bare names.
#     ⭐⭐ ALL 28 NOW CLOSED (#12244). The final three were the composite CALLERS
#     blsg2_encode, blsk_g2_wire, bnf_add_mod_p, and they confirm the union rule stated
#     above: their unions are SEMANTICALLY REQUIRED (each body `jal`s to its callee, and
#     `bnf_add_mod_p` calls bnf_be_to_le TWICE plus bnf_le_to_be once), and every
#     component is a GuestImageEntries pairing -- 2/2, 2/2 and 3/3 -- so the image
#     discharges the whole union via several pairings rather than one.
#     ⛔ A union whose extra components are NOT called is the LEAF situation instead
#     (#12389/#12516): there the union was an artifact of where the proof lived, strictly
#     stronger than the image claim, and NOT rowable. Same syntax, opposite verdict --
#     so check the emitted body's `jalOff` targets, not just the CodeReq's shape.
#     ⚠️ `bnf_add_mod_p`'s row is LAYOUT-CONDITIONAL: three of its disjointness
#     hypotheses are written against literal arena addresses (0xa0b00e80 etc.), so it
#     must be re-checked if `arenaB` moves.
#     ⭐ THE `needs-read` BUCKET, MEASURED 2026-08-17 (all 7 of it). It is NOT a residue of
#     hard proofs -- every entry was flagged for AMBIGUOUS NAMES defined in >1 file
#     (`mulCr`, `laHi`/`laLo`, `pc`, `A`, `D`/`fullCode`). Resolving per-module settles
#     each in minutes:
#       * bnf_mul_mod_p, secf_mul_mod_p -- ROWED here. `mulCr` exists in 3 modules; the
#         right one gives a 3/3 image-backed union, exactly the ADD twins' shape.
#       * secf_cmp_p, secf_sub_mod_p -- ⛔ NOT ROWABLE, and not for a proof reason: their
#         triples are anchored at `secfCmpPPc` / `secfSubModPPc` = 0x80000000, which
#         Secp256k1Field.lean itself comments as "Probe-only PC placeholder; unlinked from
#         guest". Neither symbol has a GuestAddrs def, a guestImageEntries pairing, or a
#         MANIFEST row. These are proofs about code that is not in the image.
#       * chain_validate_consecutive_numbers, chain_validate_increasing_timestamps -- ⛔
#         likewise absent from GuestAddrs and guestImageEntries; consistent with #12354
#         retiring that family. Their `fullCode` unions a `cvcn/cvit` code with an RLP
#         helper's, and `fullCode` is defined in NINETEEN files -- the ambiguity champion.
#       * u256_eq -- see its own entry: flat but position-independent, needs a base
#         instantiation plus a flatten tie at that base.
#     ⇒ so `--shape`'s three residual buckets are honest about SHAPE but say nothing about
#     LINKAGE. Always cross the shape verdict with GuestAddrs + guestImageEntries.
#     ⭐ THE `structured-only` BUCKET, MEASURED 2026-08-17 (all 8 of it) -- and the label is
#     WRONG for every one: none is a structured `.Spec`, all 8 are FLAT whole-routine
#     triples at a POSITION-INDEPENDENT base. That is a THIRD blocker class the tier column
#     has no vocabulary for, distinct from "needs Fn.retSpecFlat" and from union CodeReqs.
#       * widx_cmp32, widx_record_ptr -- ROWED (#12244) by instantiating `base` and
#         identifying the program. See Codegen/Proofs/MptWitnessIndexFlatEntry.lean.
#       * memory_expansion_gas, message_call_gas -- ⛔ NOT LINKED (calc_excess_blob_gas deleted outright; dead-code removal PR).
#       * rlp_content_to_{u256_be,u64}_strict -- ⛔ base-indexed code FAMILY + no pairing.
#     ⇒ so "position-independent base" is not one verdict but four: rowable-by-instantiation,
#     program-mismatch, unlinked, or code-family. Read the CodeReq AND check linkage.
#     ⛔⛔ THE `model-only` BUCKET (28 symbols) AND WHY "needs Fn.retSpecFlat" IS THE WRONG
#     REMEDY FOR ALL OF IT, measured 2026-08-17. `Fn.retSpecFlat` requires
#     `hpostEmp : forall rf' ws' A, f.post rf' ws' A -> A = empAssertion`, i.e. the post must
#     DETERMINE the ambient. `Fn.retSpecFlatAmbient` requires the same in the form
#     `hpostAmb : ... -> A' = A`. So the two adapters are blocked IDENTICALLY and switching
#     between them changes nothing.
#     ⇒ ZERO of the 19 linked+image-paired model-only leaves pin the ambient: every post has
#     the shape `fun _ ws _ => ...`, ignoring its third argument. The bucket is blocked on
#     amending the `Fn`s, not on picking an adapter. Compare MultiRead.multiReadFn (post pins
#     A to its two read-only inputs) and bnqZeroFn (pins A to empAssertion) -- both flatten.
#     ⭐ WORKED RECIPE, done once for bnc_zero64 (#12244, now rowed): pin the ambient in the
#     loop invariant AND in pre/post; thread the extra conjunct through the vcgen obligations
#     (mechanical); apply Fn.retSpecFlat as the zeroer rows do. Reduce `body.size` to a
#     literal in `hsz` or `decide` refuses on free variables. bncZero64Fn had no callers
#     outside its module -- CHECK THAT before amending an Fn, since pre/post are its API.
#     ⭐ THE ZEROER SUB-FAMILY IS NOW COMPLETE (3/3, #12244): bnc_zero64,
#     secp256k1_point_zero64, bnp_fp2_zero. The recipe transferred WITHOUT adjustment
#     between the two loop-shaped ones (their modules differ by an 18-line diff, all
#     docstring/import/line-wrap), and needed only one change for the straight-line
#     bnp_fp2_zero: no loop invariant to amend, and the post obligation wanted its
#     conjunction split (`refine ⟨?_, hA⟩`) instead of a threaded `hA`.
#     The remaining 16 are: bhr_rev_le_be, bnc_copy64, bnc_is_inf64, bnf_is_zero32,
#     bnp_fp2_{copy,is_zero}, bnq_{copy,is_zero,pt_copy}, enrg_u32le, p256_is_zero_n,
#     secp256k1_point_copy64, spw_u32le, ssz_pack_bytes, swr_rev_le_be, sws_u32le.
#     ⭐ COPIER TRANCHE: 3 of 5 done (#12244) -- bnc_copy64, secp256k1_point_copy64,
#     bnp_fp2_copy. The read-only `region := <src, srcBytes>` is non-empty, so the source
#     window rides through as an outer conjunct and the Region.empty collapse used for the
#     zeroers does NOT apply. Two per-module gotchas worth knowing: Bn254Fp2CopySAsm's pre
#     orders orig.length BEFORE srcBytes.length (opposite of Bn254CurveCopySAsm -- a regex
#     patch would silently swap two same-typed hypotheses), and the post obligation's
#     trailing `rfl` must become `exact <by rw [...], hA>` once the post is a conjunction.
#     ⛔ bnq_copy (384 B) and bnq_pt_copy (1152 B) are NOT size variants -- they step by
#     DWORDS (invariant `x10 = src + 8 * (i + 1)`, counter in x7, bound i < 48) where the
#     three above step by BYTES with the counter in x5. The byte-stepping patch does not
#     apply; they need their own anchors.
#     ⭐ DONE: bnq_copy and bnq_pt_copy are rowed too, so the COPIER FAMILY IS COMPLETE
#     (5/5, #12244). The dword variant needed three extra anchors beyond the byte one:
#     the `8*(i+1)` invariant, an `inv_init` with a TWO-LEVEL reachability destructure,
#     and a `mem` obligation with TWO branches (pre-entry | loop) rather than one.
#     Remaining model-only leaves: 11 -- bhr_rev_le_be, bnc_is_inf64, bnf_is_zero32,
#     bnp_fp2_is_zero, bnq_is_zero, enrg_u32le, p256_is_zero_n, spw_u32le,
#     ssz_pack_bytes, swr_rev_le_be, sws_u32le.
#     ⭐ Next natural tranche is the IS-ZERO predicates (bnc_is_inf64, bnf_is_zero32,
#     bnp_fp2_is_zero, bnq_is_zero, p256_is_zero_n): their posts constrain a REGISTER
#     (`rf.get .x10 = ...`) rather than the window, so expect the `post` projection and
#     the Q assertion to differ from both the zeroers and the copiers.
#     ⭐ IS-ZERO TRANCHE: 2 of 5 done (#12244) -- bnq_is_zero, bnp_fp2_is_zero. Confirmed
#     the geometry guess: rw is EMPTY (`ws = []`) and the read-only region carries the
#     input, the mirror image of the zeroers.
#     ⭐⭐ AND THE CLEANEST CONFIRMATION OF THE WHOLE DIAGNOSIS: their BLS12 sibling
#     Bls12Fq12IsZeroSAsm.blqIsZeroFn ALREADY pinned the ambient, which is exactly why
#     blq_is_zero was rowable earlier with NO Fn change while bnq_is_zero -- the same
#     algorithm at a different width -- was not. Once amended, that module's entire
#     flat-entry section ported over with name/width substitution and built first try.
#     ⚠️ Beware substitution collisions when porting between widths: naively rewriting
#     "8" in the Fp2 module turns `Rv64` into `RvNN` and `BitVec 8` into `BitVec NN`.
#     Substitute on qualified names and on `N <= bs.length`, never on bare digits.
#     Remaining model-only leaves: 9 -- bhr_rev_le_be, bnc_is_inf64, bnf_is_zero32,
#     enrg_u32le, p256_is_zero_n, spw_u32le, ssz_pack_bytes, swr_rev_le_be, sws_u32le.
#     ⚠️ bnc_is_inf64 / bnf_is_zero32 / p256_is_zero_n are `whileBreak`-scan-bodied and are
#     a BIGGER job than the three shapes done so far, measured 2026-08-17: SEVEN vcgen
#     obligations (inv_init, inv_step, exhausted, guard_exit, break, before.load.mem, post)
#     plus TWO named predicates to amend (`<sym>ScanInv` and `<sym>ScanPost`), not just the
#     Fn's pre/post. Deliberately not attempted in #12244's batches -- flagged, not hidden.
#     ⭐ BUT THE TEMPLATE ALREADY EXISTS AND IS THE SAME ROUTINE AT THE SAME WIDTH:
#     `secf_is_zero32` is rowed, its Fn (Secp256k1FieldIsZeroSAsm.secfIsZero32Fn) and its
#     `secfIsZeroScanInv` ALREADY pin the ambient, and its flat triple is
#     `EvmAsm.Codegen.AmbientFree.secfIsZero32FlatEntry_spec`. So `bnf_is_zero32` is that
#     module's amendment + lift ported across the curve namespace -- exactly the
#     BLS12->BN254 move that made the is-zero pair cheap. Start there, not from scratch.
#     ⚠️ `Bn254Field.lean` is a large shared file (bnf_eq32 and others follow at ~line 443),
#     so the port must be surgical rather than a whole-section copy.
#     ⭐ DONE for bnf_is_zero32 (#12244), and the port was cheaper than the 7-obligation
#     count suggests: the normalised diff between secfIsZero32Fn_spec and bnfIsZero32Fn_spec
#     was EXACTLY 14 ambient-threading edits and nothing else, so the proof body was ported
#     wholesale (with each changed line machine-verified to be its old line plus one ambient
#     token) instead of hand-editing 14 non-unique lines. The flat entry came from
#     secfIsZero32FlatEntry_spec in Codegen/Proofs/AmbientFreeFlatTriples.lean.
#     ⚠️ Two artifacts to expect when porting a proof body between files: the template may
#     lack a trailing newline if it ends its file (off-by-one in the diff pairing), and some
#     edits add an IGNORED ambient slot `, -` rather than `, hA` -- a deletion-based
#     verification rule eats the closing bracket on those.
#     Remaining model-only leaves: 8 -- bhr_rev_le_be, bnc_is_inf64, enrg_u32le,
#     p256_is_zero_n, spw_u32le, ssz_pack_bytes, swr_rev_le_be, sws_u32le.
#     ⭐ bnc_is_inf64 is DONE too (#12244) -- the same shape at 64 bytes, and the amended
#     proof differs from the 32-byte one in exactly 24 lines, ALL width digits. So this
#     family is now 4/5 (secf_is_zero32, bnf_is_zero32, bnc_is_inf64 rowed; bnq/bnp done
#     as the OR-fold variant). Only p256_is_zero_n remains, and it is the LENGTH-
#     PARAMETERISED member: its post is `isZeroNResult bs len` with `len` in a1, so expect
#     an extra register in the footprint and `len`-indexed rather than fixed-width anchors.
#     ⛔ AND A BLOCKER THE OTHER FOUR DID NOT HAVE, measured 2026-08-17: p256_is_zero_n's
#     loop invariant is NOT module-local. It is `WhileBreakDemo.scanInv` from
#     EvmAsm/Rv64/SAsm/WhileBreakDemo.lean -- SHARED Rv64/SAsm infrastructure. Pinning the
#     ambient there is a wider change than the four module-local amendments in #12244, and
#     it touches a layering-sensitive area, so it was NOT attempted. Two honest options:
#       (a) amend `WhileBreakDemo.scanInv` and thread its own demo proof too (2 references
#           in that file: the def and one `whileBreak` body at ~line 143), then p256 follows
#           the usual recipe. Cleanest conceptually; touches shared infra.
#       (b) give P256IsZeroNSAsm its OWN local invariant (a copy with `A = empAssertion`)
#           and leave the shared one alone. Contained, but duplicates a definition.
#           ⚠️ If taking (b), CHECK THE BYTE TIE: p256IsZeroNBody feeds
#           `(p256IsZeroNBody 0 [] 0).flatten 0 ++ [ret] = p256IsZeroN_prog`. Invariants are
#           proof-only annotations so flatten should ignore them, but that is exactly the
#           kind of "should" worth confirming with the kernel before relying on it.
#     ⚠️ Note also its register roles are SWAPPED relative to the other four: x5 is the
#     cursor and x6 the counter (elsewhere x6 cursor, x5 counter), and `len` arrives in x11.
#     So the 14 threading edits' anchors do not transfer verbatim even after (a) or (b).
#     Remaining model-only leaves: 6 -- bhr_rev_le_be, p256_is_zero_n, spw_u32le,
#     ssz_pack_bytes, swr_rev_le_be, sws_u32le.
#     ⇒ NOW 4 after the swr/bhr pair landed: p256_is_zero_n, spw_u32le, ssz_pack_bytes,
#     sws_u32le. Two of those four (spw/sws) are the SHARED-sgLoadU32leFn class and
#     p256_is_zero_n is the shared-scanInv class, so only ssz_pack_bytes is a plain leaf.
#     ⇒ NOW 2 after spw/sws (+eph, which was never in this tally) landed: p256_is_zero_n
#     and ssz_pack_bytes. See the correction below -- the "shared-sgLoadU32leFn class" was
#     a misreading, and only p256_is_zero_n is genuinely shared-definition.
#     ⇒ NOW 1: only p256_is_zero_n remains, and it is the one genuinely shared-DEFINITION
#     case (`WhileBreakDemo.scanInv` is `Rv64/SAsm` infrastructure used AS the invariant,
#     not delegated to in a proof). Every leaf whose blocker was a leaf change is done.
#     ✅✅ NOW 0 -- p256_is_zero_n is DONE and ROWED (#12244). THE MODEL-ONLY BUCKET IS
#     EMPTY: all 19 linked model-only leaves measured at the start of #12244 now carry
#     guest-image-anchored flat triples.
#     ⭐ It WAS the genuine shared-definition case, but the blast radius I recorded was
#     OVERSTATED, which is the fourth and last correction in this series. `scanInv` has
#     exactly THREE references in the tree: its own def, `scanNzFn`'s body, and
#     `p256IsZeroNBody`. Every OTHER external use of WhileBreakDemo is of the pure `nlz`
#     spec function and its lemmas -- NOT the invariant. So "Rv64/SAsm infrastructure"
#     was two consumers, one of them the demo itself, and pinning it cost FOUR edits there.
#     ⇒ ⭐⭐ THE MECHANICS FINDING THAT WOULD HAVE SAVED THE MOST TIME ACROSS THIS WHOLE
#     STACK: only sites that BUILD an enlarged predicate need editing. `rcases`/`rintro`
#     patterns TOLERATE A SHORT LIST -- the final binder absorbs the remaining conjunction
#     -- so a destructure keeps working when an invariant grows a trailing conjunct.
#     p256's own 8-obligation proof needed ZERO edits: its `rintro` bundled the new tail
#     and its `refine` consumed that same bundle. The demo needed 4 (2 destructures that
#     had to SPLIT the bundle to name `hA`, and the 2 `scanPost` builds). I threaded all
#     28 sites in ssz_pack_bytes defensively; most were probably unnecessary. Try
#     definitions-only first and let the compiler name the sites that actually break.
#     ⭐⭐ CREDIT: the module header of Codegen/Proofs/AmbientFreeFlatTriples.lean ALREADY
#     recorded this whole diagnosis BY NAME for enrg_u32le / spw_u32le / sws_u32le --
#     "the same computation as bahU32leFn but their posts read `fun rf _ _ => ...`,
#     discarding the ambient binder entirely. Those are unliftable until their contracts
#     are pinned -- a leaf change, not a lift." Written before #12244. What #12244 adds is
#     the measurement that this holds for ALL 19 linked model-only leaves, not three, and
#     13 worked leaf changes.
#     ⭐ enrg_u32le is DONE: bahU32leFn was already pinned and bah_u32le already rowed, so
#     pinning enrg's own Fn let bahU32leFlat_spec port over by name substitution.
#     ✅ spw_u32le / sws_u32le / eph_u32le are ALSO DONE (#12244) -- entries REMOVED below.
#     ⛔⛔ CORRECTION, and this one I got wrong TWICE in this file, so the reasoning error
#     matters more than the result. I wrote: "their Fn_specs are `simpa ... using
#     sgLoadU32leFn_spec`, i.e. they DELEGATE to a SHARED `sgLoadU32leFn` used by FIVE
#     modules ... Pinning them means pinning the shared Fn and re-checking all five
#     wrappers -- the same shared-definition class as p256_is_zero_n's scanInv, NOT a leaf
#     change." THAT WAS WRONG, and the error was conflating "the spec PROOF delegates to X"
#     with "the Fn IS X".
#       * `spwU32leFn` / `swsU32leFn` / `ephU32leFn` are three SEPARATE `Fn` definitions
#         that merely had field-wise identical contents to `sgLoadU32leFn`. Only the proof
#         was coupled: `simpa [thisFn, sgLoadU32leFn] using sgLoadU32leFn_spec p bs hwf`.
#       * `BlockAccessListHashSAsm.bahU32leFn` is the SAME `Fn` with the ambient ALREADY
#         pinned -- same `region := ⟨p, bs⟩`, same `body := sgLoadU32leBody`. So each
#         wrapper pins its OWN pre/post and re-points its delegation at bah's twin.
#       * `SgLoadU32leSAsm.lean` does not appear in the diff. Blast radius: ZERO. Verified
#         by `git diff --name-only`, not asserted.
#     All three built on the first attempt, and the lift is `enrgU32leFlat_spec` ported by
#     name substitution.
#     ⇒ Transferable rule: READ WHAT A SHARED SYMBOL IS USED FOR before pricing the change.
#     A delegated PROOF is not a shared DEFINITION. (Same family of error as assuming a
#     3-register split had no template -- both were "I priced it by resemblance instead of
#     by reading it".)
#     ⚠️ p256_is_zero_n is NOT resolved by this: `WhileBreakDemo.scanInv` is genuinely a
#     shared DEFINITION in Rv64/SAsm infrastructure, referenced as the invariant itself
#     rather than delegated to in a proof. That distinction is exactly the one this
#     correction is about, so it is worth stating which side each case falls on.
#     ✅ swr_rev_le_be / bhr_rev_le_be: BOTH DONE and ROWED (#12244) -- entries REMOVED
#     below, exemption discharged rather than moved. Two halves:
#       (a) `swrRevLeBeFn` / `swrRevLeBeInv` ambient-pinned. That ONE amendment served BOTH
#           routines because `bhrRevLeBeFn` is a definitional ALIAS of it
#           (`:= swrRevLeBeFn ...`), `bhrRevLeBeFn_spec` delegates, and
#           `bhrRevLeBe_prog = swrRevLeBe_prog` -- the same code at two guest addresses. A
#           shared Fn that did NOT fan out, unlike sgLoadU32leFn's five modules.
#       (b) ONE base-parameterized lemma `revLeBeFlat_at` (Codegen/Proofs/
#           RevLeBeFlatTriples.lean), instantiated at both `GuestImageEntries` pairings with
#           `hprog := rfl`. That the same lemma accepts both bases IS the byte-identity
#           witness: the body's `flatten` is base-independent.
#     ⛔⛔ CORRECTION to what this file previously said, worth keeping because the error was
#     mine and it cost a reverted proof: I recorded that these were "harder than every
#     template in this stack" because the routine pins THREE registers (a0=src, a1=len,
#     a2=dst) and "every copier/zeroer/is-zero template pins at most two". THAT WAS WRONG.
#     `U256BeFlat.exposedRegs_split_add` splits the SAME fifteen exposed registers around
#     the SAME three registers, and the tree also has 4-, 5- and 10-register splits
#     (CallFrameCalldataFlatTriple `exposedRegs_split_4`, Secp256k1PointDoubleSAsmStage
#     `exposedRegs_split5`, Bn254FieldMulModPSAsmStage `exposedRegs_split10`). The lift
#     took one build attempt once ported from the 3-register template.
#     ⇒ The real lesson is NOT "3 registers is hard", it is: GREP FOR THE MECHANISM
#     (`exposedRegs_split`) BEFORE JUDGING NOVELTY. I judged by the shapes I had already
#     ported instead. My hand-written attempt failed for a reason the template shows how to
#     avoid: it bound the if-valuation with `set rf0`, which puts the routine's arguments in
#     the expected type of the `Fn.retSpecFlat` application ("expected type must not contain
#     free variables"). The template writes that valuation INLINE and builds `hpre` as a
#     separate `have` with `show RegFile.get _ .x10 = ...`.
#     ⚠️ `hdisj` is load-bearing for a subtle reason worth keeping (now in the new module's
#     header too): the block engine's `inRw` routing test is ARITHMETIC, so without src/dst
#     disjointness an `LBU` aimed at the source can be routed to the writable window and
#     read a partially-reversed byte. Not just a framing convenience.
#     ✅ ssz_pack_bytes is DONE and ROWED (#12244) -- entry REMOVED below. Both halves
#     (28 anchored threading edits, then the lift) built on the FIRST attempt.
#     ⛔⛔ TWO CORRECTIONS to the measurement that used to be in this block, both found
#     only by opening the file instead of trusting my own note:
#       (1) I wrote "TWO `Fn`s share the invariants IN THIS FILE ... pinning `copyInv`
#           therefore forces the Copy spec's four obligations too". FALSE. The second
#           spec (`copyLoopFn_spec`) sits inside a `/- ... -/` BLOCK COMMENT at lines
#           382-459 -- dead code. Seven of the 22 destructure sites I had counted do not
#           exist, and there is exactly ONE live `Fn`. Lesson: when counting sites by
#           grep, CHECK WHETHER THEY ARE IN A COMMENT.
#       (2) The remaining count was right in magnitude (28 live anchored lines) but the
#           difficulty inference from it was wrong -- every edit was mechanical. Size of a
#           mechanical diff is not difficulty; that is the third time on this queue I
#           deferred on a proxy for difficulty instead of on difficulty.
#     ⭐ What WAS true, load-bearing, and worth keeping: pinning only the `Fn`'s pre/post
#     does NOT work, because `case sszPackBytes.post` receives the ambient solely through
#     the strongest-post hypothesis `hsp`, which routes across BOTH loop boundaries. So
#     `copyInv` and `padInv` must each carry `A = empAssertion` themselves. Confirmed by
#     trying the shortcut first.
#     ⚠️ Edit-mechanics note for the next two-loop routine: the tuple SUFFIXES repeat
#     across obligations (`hsrcLen, horig, hsrc, hdst, hdj, hwin⟩` occurs at four distinct
#     sites), so string-replace hits the wrong one -- edit by LINE NUMBER with an asserted
#     expected value. And use `-` not a named binder for slots you do not consume, or
#     `check-no-warnings` fires on the unused variable.
#     ⇒ The lift needed TWO register splits, one per direction, because the contract is
#     asymmetric: `exposedRegs_split_pack3` for the pre (three pinned ABI regs, 12 scratch)
#     and `exposedRegs_split_pack1` for the post (only `a0` known -- it publishes
#     `chunkCount len` -- so 14 owned). That is the `u256_add_be` published-result shape
#     crossed with the reverse-copy's three-register pre, exactly as predicted below.
#
#     [historical: the measurement this block used to carry]
#     ⚠️ ssz_pack_bytes is the heaviest of the bucket, and here is the MEASURED size
#     rather than an adjective (#12244, counted -- not estimated):
#       * 26 anchor lines need the ambient threaded: 4 definition sites
#         (`copyInv` 267, `padInv` 280, and `sszPackBytesFn`'s `pre` 478 / `post` 484,
#         all currently `fun ... _ =>`) plus 22 destructure/rebuild sites naming
#         `hsrcLen`/`horig` (298 307 340 343 394 395 403 413 443 549 587 591 603 611
#         670 748 765 783 786 800 838 872). For scale: the swr/bhr amendment was 7
#         edits and the largest earlier one in this stack was 14.
#       * TWO `Fn`s share the invariants IN THIS FILE: `sszPackBytesFn` and
#         `sszPackBytesCopyFn`. Pinning `copyInv` therefore forces the Copy spec's four
#         obligations too, so its `pre` must be pinned as well or its `inv_init` cannot
#         discharge `A = empAssertion`. (The `padInv` in `Rv64/SAsm/ZeroPadLoop.lean` is
#         a DIFFERENT, unrelated definition -- do not chase it.)
#     ⚠️ CONFIRMED, so nobody retries the shortcut I tried first: pinning ONLY the `Fn`'s
#     pre/post does NOT work. `case sszPackBytes.post` receives the ambient solely through
#     the strongest-post hypothesis `hsp`, which routes across both loop boundaries, so the
#     `A = empAssertion` fact is lost unless the INVARIANTS carry it. That is why the
#     invariant edits are unavoidable and the count above is the real count.
#     ⇒ Shape of the eventual lift, already precedented: geometry is the COPIER one
#     (non-empty `region := ⟨src, srcBytes⟩` AND non-empty `rw := ⟨dst, outLen len⟩`), and
#     it pins THREE ABI registers (a0/a1/a2) while its post PUBLISHES a0 as
#     `chunkCount len`. So it needs the u256AddBe treatment: a 3-way
#     `exposedRegs_split` for the pre plus a 1-way split for the post (the post pins only
#     a0), exactly the `exposedRegs_split_a0_a1` + `exposedRegs_split_a0` pairing in
#     Codegen/Proofs/AmbientLiftedFlatTriples.lean. Templates: `u256AddBeFlat_spec` for the
#     published-result post, `RevLeBeFlat.revLeBeFlat_at` for the 3-register copier pre.
#     ⚠️ And `hdisj` will be load-bearing for the same arithmetic-`inRw` reason as the
#     reverse-copy -- the Fn already carries the disjointness hypothesis, so keep it.
#     ⚠️ 8 further model-only symbols are NOT LINKED at all (copy_word_gas, init_code_cost,
#     keccak256_word_gas, log_data_gas, nibbles_common_prefix_len, rlp_field_to_u256_be,
#     running_bloom_{copy,zero}) -- no lift makes those the image claim.
#     They were left for a second pass on purpose: their CodeReqs are stage UNIONS or
#     file-local base abbrevs rather than a bare `CodeReq.ofProg (GuestAddrs.<sym>)`,
#     so each needs the union checked against the guest image before a row is honest.
#     The tier column is not evidence — `--shape` is the queue, the statement is the
#     oracle, and `GuestImageEntries` is what says the CodeReq is the REAL code.
#   `check-registry-coverage.py` still computes its own tier by theorem-name SUFFIX
#   (`_spec_within` / `Flat_spec`, see `tier_a` in that script), which is a proxy for
#   shape: it misses a flat triple whose name ends `_spec` (the 19 above, and the
#   `u256_lt_be` case). Switching that gate onto the #12226 parser is the follow-up.

# ⚠️ THE PARENTHESISED COUNTS IN THE TWO SECTION HEADERS BELOW HAD DRIFTED, and
#   nothing checks them: before #12244 removed the bnf pair they read (9) and (68)
#   against actual contents of 5 and 56.  They are prose, like the tier column, and
#   the same rule applies — count the lines, do not read the header.  Corrected here.
#
# --- TIER A: flat triple at the guest address; registrable now (0) ---
# ⭐ THIS SECTION'S ONE TIER-A MEMBER IS GONE as of #12244: `eip8037_tx_state_gas` is
#   rowed `.proven` in Progress/Routines.lean citing `eip8037TxStateGas_spec_within`,
#   so its exemption was deleted here.  Its reason was a CODE-IDENTITY claim ("no
#   `guestImageEntries` pairing ... and no `eip8037TxStateGasFunction` row in
#   MANIFEST.tsv") that was TRUE WHEN WRITTEN (2026-08-17) and that the linking
#   ledgers satisfied afterwards (`guestImageEntries` pairing 2026-08-25, MANIFEST row
#   2026-08-26) — a NEW way for an entry to go stale, distinct from the false-shape
#   claims above, and one only a re-read of BOTH halves (shape and code identity)
#   catches.
#   ⚠️ The two `bal_serializer_*` lines that remain under this header grade themselves
#   tier B in their own reasons; they are not tier-A debt, which is why the header
#   count reads 0 with two lines below it.
#   ⚠️ AND the header count is about THIS SECTION only — it is NOT the gate's tally.
#   `check-registry-coverage.py` computes its own tier-A proxy from the theorem NAME
#   and, after this change, still reports 1 tier-A: `header_validate_base_fee`, whose
#   entry sits under a different header in this file.  Sections and the gate's grade
#   have never agreed here; read the gate's output, not the headers.
bal_serializer_balance_to_le	tier B — `balanceToLeFn_spec` (Codegen/Programs/BalSerializerLeSAsm.lean) is a structured `Fn.SpecR` at the pinned guest placement `GuestAddrs.bal_serializer_balance_to_le`, proof-first (DCode) generated code with `blockA`/`la`. SpecR is the CALLER-SHAPED (`Stmt.soundR`) contract — `Fn.retSpecFlat` flattens only the leaf (`Fn.Spec`) path, so no flat whole-routine triple exists yet; deriving a SpecR→flat lift (or restating the routine's post at `cpsTripleWithin` level as the `abiFrame` consumers do) is the unblock (#12736)
bal_serializer_slot_to_le	tier B — same class as `bal_serializer_balance_to_le` directly above: `slotToLeFn_spec` is a structured `Fn.SpecR` at `GuestAddrs.bal_serializer_slot_to_le` (the twin instantiation of the same DCode derivation, `bslDeriv`); blocked on the same SpecR→flat lift (#12736)

# ⚠️ THE TWO `rlp_content_to_*_strict` ENTRIES BELOW ARE NOT "structured SAsm spec only"
#   despite sitting under this header — both are flat whole-routine triples at a FREE
#   `base`, so their remedy is instantiation + a `guestImageEntries` pairing, NOT
#   `Fn.retSpecFlat`. They are here because this is where non-tier-A entries live; read
#   each entry's own reason, not the header (see the tier-drift warning at the top).
#
# --- TIER B: structured SAsm spec only; needs Fn.retSpecFlat first (8) ---
bal_serializer_balance_to_le	tier B — `balanceToLeFn_spec` is a complete, discharged caller-shaped `Fn.SpecR` at `GuestAddrs.bal_serializer_balance_to_le` (the generated derivation has no `DStmt.call`; only `hreg`/`hrw` are explicit). It is not yet a flat `Fn.Spec`/`cpsTripleWithin` contract consumable by `Progress/Routines.lean`; row only after a flat whole-routine triple is derived and consumed at this concrete placement, with image identity tied to the emitted program.
bal_serializer_slot_to_le	tier B — `slotToLeFn_spec` is the corresponding complete, discharged caller-shaped `Fn.SpecR` at `GuestAddrs.bal_serializer_slot_to_le` (no applied-callee premises; only `hreg`/`hrw`). It is not yet a flat `Fn.Spec`/`cpsTripleWithin` contract consumable by `Progress/Routines.lean`; row only after a flat whole-routine triple is derived and consumed at this concrete placement, with image identity tied to the emitted program.
call_frame_forward_gas	tier B — `callFrameForwardGasFn_spec` (Codegen/Programs/CallFrameForwardGasSAsm.lean) is the structured DCode/SAsm contract used to generate the linked oracle Program; no flat whole-routine `cpsTripleWithin` exists at `GuestAddrs.call_frame_forward_gas` yet, so do not grade it `.proven` until the `Fn.retSpecFlat` lift is derived (#12853)
erh_hash_one	tier B — linked and spec-bearing (`erh_hash_one_spec_within_empty` and `_nonempty`) but invisible to the registry gate until the detector widened; needs a registry row.

witness_codes_lookup_by_hash	tier B — linked and spec-bearing (`witness_codes_lookup_by_hash_spec_within_empty_section`) but invisible to the registry gate until the detector widened; needs a registry row.
witness_lookup_by_hash_indexed	tier B — linked and spec-bearing (`witness_lookup_by_hash_indexed_spec_within_empty` and `_one_hit`) but invisible to the registry gate until the detector widened; needs a registry row.
header_validate_base_fee	heq is ALREADY discharged in-tree: header_validate_base_fee_spec_within (Codegen/Programs/HeaderValidateBaseFeeSpec.lean, "Complete K74 wrapper") carries only hk73 as a callee-triple premise; the equality call composes locally from the verified u256EqBody adapter (header_validate_base_fee_eq_call_spec_within, HeaderValidateBaseFeeSpecCore.lean, takes only heqMono code subsumption + static region facts — no heq triple). Sole remaining blocker = hk73: discharging it via k73_incr_route_adapter requires the K74 wrapper premise to carry the mul-callee scratch frame (k73_incr_env = frameSlots(spK-48) f0..f5 + acc region + x14..x17 owns, vs k74FlatFrame = regOwns [.x14..x17] only) — a wrapper-premise shape change — and K73's own static gates stay open (ABI facts; 0 < target = issue #12951). (#12346).
