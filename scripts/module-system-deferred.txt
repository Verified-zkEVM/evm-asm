# Modules deliberately held back from module-system waves, with the reason.
#
# A deferral costs its whole REVERSE cone (a `module` file cannot import a
# non-`module` one), so each line here is expensive and must say why. Empty this
# file as the reasons are resolved; `--blocked` prints the current cost.
#
# One module per line, `#` comments and blank lines ignored.
#
# ---------------------------------------------------------------------------
# CURRENTLY EMPTY.
#
# `EvmAsm.Codegen.Emit` was the only entry and is resolved: it now owns exposed
# renderers (`natStr`, `intStr`, `joinLines`, `emitReg`) instead of core's
# `Nat.repr` / `String.intercalate` / `ToString Reg`, none of which are exposed.
# That released a reverse cone of 1037 modules -- a third of the tree.
#
# The 24 modules that still cannot migrate are NOT deferrals: they are the
# reverse cone of the vendored Sail `Out`, which is not ours to migrate. They
# are detected from the tree by `blocked_modules()`, not listed here, so they
# cannot go stale. `--blocked` shows them.
