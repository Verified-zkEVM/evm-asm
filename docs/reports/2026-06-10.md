# evm-asm Status Report

**Date:** 2026-06-10  **Commit (main):** `2730a3f9b`

## Executive summary

evm-asm is building a **formally verified EVM state transition function as RISC-V (RV64IM)
macro-assembly**, intended to run as a stateless block-validator guest program inside a zkVM.
The opcode-verification layer is the most mature: **42 EVM opcodes carry complete,
kernel-checked correctness proofs** (full stack-level Hoare triples), with a trusted base of
only the three standard classical axioms — no `native_decide`/`bv_decide`, no `sorry`. A
parallel **codegen track** emits runnable RV64 and wires **154 opcode bytes** through a tiny
interpreter registry, and a pure-Lean **execution-layer (EL)** models accounts, storage,
transactions, calls and creates, exercised by a **66-vector conformance suite (55 passing, 0
unexpected failures)**. The gap between "verified opcodes" and "verified zkEVM" is the
interpreter loop and full opcode coverage: of the **9 guest-program obligations, 2 are done and
6 are blocked** on a not-yet-shipped RV64 fetch/decode/dispatch loop and the opcodes still
lacking RV64 subroutines.

## 1. EVM Opcodes

**85 registry entries / 149 opcode bytes** modeled, tracked in a kernel-checked registry
(`EvmAsm/Progress.lean`, CI-gated against drift).

| Tier | Entries | Bytes | Meaning |
|---|---:|---:|---|
| ✅ Proven | 42 | 72 | Complete stack-level Hoare triple, no input restriction |
| 🔶 Conditional | 2 | 2 | Complete triple, gated by an input-domain precondition |
| 🟡 Partial | 6 | 36 | Arithmetic lemma / sub-component only — no full triple yet |
| ⏳ ExecSpec | 32 | 36 | Executable-spec semantics only — no RV64 subroutine |
| ✗ Not started | 3 | 3 | Not yet modeled in the opcode enum |

**Fully proven** spans arithmetic (ADD, SUB, MUL, DIV, SIGNEXTEND), all bitwise
(AND/OR/XOR/NOT), all shifts (SHL/SHR/SAR), all comparisons (LT/GT/SLT/SGT/EQ/ISZERO), BYTE,
and the stack family (POP, PUSH0, DUP1–16, SWAP1–16). **SDIV** and **MOD** have complete triples
gated by an input-domain precondition (🔶 conditional — e.g. MOD's divisor restricted to
`b.getLimbN 3 = 0`). The remaining arithmetic with hard 256-bit semantics — **SMOD, MULMOD,
ADDMOD, EXP** — plus **CALLDATACOPY** and **PUSH2–32** have verified arithmetic lemmas or
partial-effect specs but not yet a full triple (🟡 partial).

**Trust base:** every proof is kernel-checkable. `#print axioms` over the witnessed theorems
shows **0 axioms beyond `propext`/`Classical.choice`/`Quot.sound`**; the forbidden-tactic and
axiom CI gates keep `native_decide`/`bv_decide` out.

## 2. State Transition Function (STF)

The execution layer (`EvmAsm/EL/`, ~179 files) specifies the STF as pure Lean, independent of
RISC-V. Three maturity levels matter here and are tracked separately below:

- **Modeled** — data structures / predicates are *defined* (the shape), but no computation runs.
- **Executable** — a computable Lean `def` that actually evaluates.
- **Proven** — theorems establish correctness (mutation/frame lemmas, conformance checks).

| Area | Modeled | Executable | Proven |
|---|---|---|---|
| **World state** (accounts, balance, nonce, code, storage) | `Account`, `WorldState` | get/set account · storage · balance · nonce · code | ✅ read-after-write + field-isolation (frame) lemmas |
| **Transactions** (EIP-1559) | `Transaction`, `validatesAgainst` predicate | intrinsic gas, effective gas price / priority fee, upfront cost, post-exec gas refund | ✅ intrinsic-gas, fee, and refund correctness |
| **Message calls** (CALL/STATICCALL/DELEGATECALL) | `CallKind`, `CallFrame`, `CallResult` | precheck decision (balance / static / depth / stipend), value transfer, committed-state & output selection | ✅ precheck branches, value-transfer balance preservation |
| **Contract creation** (CREATE/CREATE2) | `CreateKind`, `CreateRequest` | precheck decision (size / balance / nonce / collision), request→address-input | ✅ precheck branch lemmas |
| **Logs** (LOG0–4) | `LogEntry`, `LogState` | append, topic-count check, data buffer | ✅ append + topic-validity lemmas |
| **Gas schedule** | per-opcode + dynamic (memory / copy / keccak) cost | intrinsic + EXP / calldata / keccak cost helpers | ⚠️ helpers checked by conformance; schedule **not** proven equal to the yellow paper |
| **Block-level STF** | `BlockHeader`, `BlockInput`, `BlockResult` | `BlockTransition.run` — folds txs through an executor | ⚠️ fold laws + empty-block case only |

**Two honest gaps in the chain:**

1. **The call/create executors are abstract hooks, not implementations.** `CallExecutor`
   (message-call execution) and `AddressDeriver` (keccak/RLP address derivation) are
   function-typed *parameters*. The EL models the shape and invariants around them and proves
   the surrounding bookkeeping; the opcode-driven execution that fills them in lives in the
   Evm64 layer and the not-yet-shipped interpreter loop. So **there is no closed, executable
   `state_transition(block, pre_state) → post_state` yet** — `BlockTransition.run` threads an
   executor it does not itself provide.
2. **Gas/cost modeling is a verified surrogate, not a proven consensus match.** Per-opcode
   `cpsTripleWithin N` step bounds are proven, but their mapping to the EVM gas schedule is
   *modeled*, not proven equal to the yellow paper (`DRIFT.md`).

**Conformance — the executable-and-proven evidence.** A kernel-checked suite of **66 vectors
(55 passing, 11 expected errors, 0 unexpected failures)** (`EvmAsm/EL/Conformance/All.lean`)
runs real EL/Evm64 executables and checks their output by `decide`/`rfl`, with a CI floor that
fails the build on regression. Coverage spans CALL output, calldata, code, CREATE initcode gas,
EXP arithmetic + gas, KECCAK256, LOG data + stack, RLP, return data, signed arithmetic
(SDIV/SMOD edges), storage (SLOAD/SSTORE), and terminating ops (RETURN/REVERT).

**Reference-semantics caveat:** correctness is measured against the pinned
`ethereum/execution-specs`; that the spec faithfully encodes consensus is assumed, not proven here.

**Guest-program obligations (the path to a complete zkEVM):** 9 total — **2 done, 6 blocked, 1
not started**.

| # | Obligation | Status |
|---|---|---|
| 2 | `read_input`/`write_output` IO interface | ✅ done |
| 1 | RV64 ELF on the `zicclsm` target | 🟡 blocked (codegen emits one extension off) |
| 3 | RLP-decode the (block, witness) input | 🟡 blocked (RV64 RLP decoder in progress) |
| 4 | EVM interpreter loop | 🟡 blocked (the keystone — not yet shipped) |
| 5 | Full opcode coverage with verified handlers | 🟡 blocked (partial/execSpec opcodes lack RV64) |
| 6 | Accelerator ECALL bridges (precompiles) | 🟡 blocked |
| 7 | MPT verification of pre-state witnesses | ✗ not started |
| 8 | Verified post-state root → public output | 🟡 blocked (on #4–#7) |

**Recent progress:** in-execution balance debit on value-bearing CALL, the LOG full-data
capture buffer, and SELFBALANCE reading the live (post-debit) balance.

## 3. Codegen

The codegen track turns verified Lean `Program`s into runnable RISC-V:

- Emits RV64 assembly text, assembled/linked for and run on the **Zisk emulator** (`ziskemu`).
- **154 opcode bytes wired** through `tinyInterpRegistry` — 100% of the live opcode space is
  routed (unused bytes fall through to INVALID).
- **Verification of the lowering** (codegen is *unverified by design*, fenced rather than
  proven): Phase-1 registry invariants are kernel-checked; **13 of ~87** handler-level specs are
  proven. The safety net is round-trip `#guard` coverage, the conformance floor, and a
  boundary-biased arithmetic differential fuzzer.
- **Caveat: 100% wired ≠ 100% spec-compliant.** Child-frame opcodes (CALL/CREATE family),
  account/block reads (BALANCE, EXTCODE\*, BLOCKHASH), and LOG are still no-ops or placeholders.
  EEST conformance currently yields **0 full-output matches** (output-encoding mismatch), though
  the stateless harness correctly *rejects* invalid blocks.

## What's next

- **Ship the interpreter loop** (codegen M5) — the keystone that unblocks obligations #4 and #8.
- **Close the conditional/partial opcodes** — full-domain triples for MOD (n=4), SDIV, SMOD,
  MULMOD, ADDMOD, EXP; promote execSpec-only opcodes to real RV64 handlers (obligation #5).
- **Reconcile EEST output encoding** to land the first full-output matches, then wire an EEST
  smoke subset into CI.
- **Grow codegen handler specs** — instantiate the remaining ~74 Phase-4 specs to shrink the
  unverified surface.
