/-
  K67 `header_validate_post_merge` — post-loop phase contracts.
  The implementation is split into the pre-merge phases and the merged
  station/branch composition so each source file stays within the review cap.
-/
import EvmAsm.Codegen.Programs.HeaderValidatePostMergePostLoopPhasesCore
import EvmAsm.Codegen.Programs.HeaderValidatePostMergePostLoopPhasesMerged
