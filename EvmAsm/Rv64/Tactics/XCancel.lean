-- GENERATED by scripts/gen-rv64-shims.py -- do not edit by hand.
--
-- `Tactics.XCancel` now lives in the riscv-zkvm dependency, prebuilt.
-- This shim re-exports it so evm-asm's existing references keep resolving;
-- `export` aliases the SAME constants, so no statement changes meaning.
--
-- Only the export block below is regenerated. The prelude between the
-- markers is preserved verbatim and is the source of truth -- aliases do not
-- propagate transitively, so those import edges are what let downstream files
-- still see the names they used to. Edit it here if the imports must change.
--
-- Regenerate with `scripts/gen-rv64-shims.py`; `--check` gates it in CI.
-- BEGIN shim prelude (preserved from the original module)
/-
  EvmAsm.Rv64.Tactics.XCancel

  Separation logic cancellation tactic, built on the XPerm infrastructure.

  ## Usage

  ```
  -- Given hypothesis h : (A ** B ** C ** D) s and goal (A ** C ** ?Frame) s:
  xcancel h
  -- Closes the goal, unifying ?Frame with (B ** D)
  ```

  ## Algorithm

  Given `h : (P₁ ** P₂ ** ... ** Pₙ) s` and goal `(Q₁ ** ... ** Qₘ ** ?F) s`:
  1. Flatten both sides to atom lists
  2. Match each concrete Qⱼ against some Pᵢ (using `isDefEq`), consuming it
  3. Compute the frame as unmatched P atoms
  4. Unify ?F with the frame
  5. Close the goal via a permutation proof
-/

module

public import Lean
public import EvmAsm.Rv64.Tactics.XPerm
meta import Lean
meta import EvmAsm.Rv64.Tactics.XPerm
public import RiscvZkvm.Rv64.Logic.Tactics.XCancel
meta import RiscvZkvm.Rv64.Logic.Tactics.XCancel

@[expose] public section
-- END shim prelude

namespace EvmAsm.Rv64

export RiscvZkvm.Rv64 (Tactics.buildSepConjChain Tactics.tacticXcancel_)

-- Namespaces downstream files `open`; see render() for why these
-- cannot come from `export`.
namespace Tactics end Tactics

end EvmAsm.Rv64
