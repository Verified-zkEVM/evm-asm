-- GENERATED by scripts/gen-rv64-shims.py -- do not edit by hand.
--
-- `Program` now lives in the riscv-zkvm dependency, prebuilt.
-- This shim re-exports it so evm-asm's existing references keep resolving;
-- `export` aliases the SAME constants, so no statement changes meaning.
--
-- Only the export block below is regenerated. The prelude between the
-- markers is preserved verbatim and is the source of truth -- aliases do not
-- propagate transitively, so those import edges are what let downstream files
-- still see the names they used to. Edit it here if the imports must change.
--
-- Regenerate with `scripts/gen-rv64-shims.py`; `--check` gates it in CI.
-- BEGIN shim prelude (preserved from the original module)
/-
  EvmAsm.Rv64.Program

  Programs as sequences of instructions, with sequential composition.
  64-bit variant of EvmAsm.Program.
-/

module

public import EvmAsm.Rv64.Instructions
public import RiscvZkvm.Rv64.Program

@[expose] public section
-- END shim prelude

namespace EvmAsm.Rv64

export RiscvZkvm.Rv64 (ADD ADDI ADDIW AND')
export RiscvZkvm.Rv64 (ANDI AUIPC BEQ BGE)
export RiscvZkvm.Rv64 (BGEU BLT BLTU BNE)
export RiscvZkvm.Rv64 (DIV' DIVU EBREAK ECALL)
export RiscvZkvm.Rv64 (FENCE HALT JAL JALR)
export RiscvZkvm.Rv64 (LB LBU LD LH)
export RiscvZkvm.Rv64 (LHU LI LUI LW)
export RiscvZkvm.Rv64 (LWU MUL' MULH MULHSU)
export RiscvZkvm.Rv64 (MULHU MV NOP OR')
export RiscvZkvm.Rv64 (ORI Program Program.getElem?_append Program.length_append)
export RiscvZkvm.Rv64 (REM' REMU SB SD)
export RiscvZkvm.Rv64 (SH SLL SLLI SLT)
export RiscvZkvm.Rv64 (SLTI SLTIU SLTU SRA)
export RiscvZkvm.Rv64 (SRAI SRL SRLI SUB)
export RiscvZkvm.Rv64 (SW XOR' XORI execProgram)
export RiscvZkvm.Rv64 (execProgram.eq_1 execProgram.eq_2 execProgram.eq_def execProgram_append)
export RiscvZkvm.Rv64 (execProgram_cons execProgram_nil execProgram_seq instAppendProgram)
export RiscvZkvm.Rv64 (prog_skip seq single «term_;;_»)

-- Namespaces downstream files `open`; see render() for why these
-- cannot come from `export`.
namespace Program end Program
namespace execProgram end execProgram

end EvmAsm.Rv64
