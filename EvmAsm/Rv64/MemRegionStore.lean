-- GENERATED by scripts/gen-rv64-shims.py -- do not edit by hand.
--
-- `MemRegionStore` now lives in the riscv-zkvm dependency, prebuilt.
-- This shim re-exports it so evm-asm's existing references keep resolving;
-- `export` aliases the SAME constants, so no statement changes meaning.
--
-- Only the export block below is regenerated. The prelude between the
-- markers is preserved verbatim and is the source of truth -- aliases do not
-- propagate transitively, so those import edges are what let downstream files
-- still see the names they used to. Edit it here if the imports must change.
--
-- Regenerate with `scripts/gen-rv64-shims.py`; `--check` gates it in CI.
-- BEGIN shim prelude (preserved from the original module)
/-
  EvmAsm.Rv64.MemRegionStore

  Writing a byte into a `bytesRegion`: `bytesRegion_sb_within`, the `SB` counterpart
  to `bytesRegion_lbu_within`. Storing byte `i` (`SB`) turns `bytesRegion regionBase bs`
  into `bytesRegion regionBase (bs.set i b)` — the writable destination a byte-array
  copy loop needs.

  The structural core is `bytesRegion_dword_at_set`: a single induction that frames the
  `i`-th dword for BOTH `bs` and `bs.set i b` with the SAME surrounding `front`/`rest`,
  so the `SB` (which updates only that dword, via `packBytes_set`) lands exactly on the
  `bs.set i b` region.
-/

module

public import EvmAsm.Rv64.MemRegionWrite
public import EvmAsm.Rv64.MemRegion
public import RiscvZkvm.Rv64.Logic.MemRegionStore

@[expose] public section
-- END shim prelude

namespace EvmAsm.Rv64

export RiscvZkvm.Rv64 (bytesRegion_dword_at_set bytesRegion_sb_within)

end EvmAsm.Rv64
